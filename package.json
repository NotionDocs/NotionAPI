{
  "name": "notionapi",
  "private": "true",
  "scripts": {
    "build": "lerna run build",
    "prepare": "yarn build",
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "clean": "rm -rf packages/**/dist",
    "client": "yarn workspace @notionapi/client",
    "core": "yarn workspace @notionapi/core",
    "transform": "yarn workspace @notionapi/transform"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "jest":  "^26.6.3",
    "lerna":  "^4.0.0",
    "microbundle": "^0.13.0",
    "prettier":  "^2.2.1",
    "ts-jest": "^26.5.0",
    "typescript": "^4.2.3"
  }
}
